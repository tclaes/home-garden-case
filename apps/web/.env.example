# Base URL of the Home Garden API.
API_BASE_URL=http://localhost:3000

# Per-user sessions are httpOnly cookies set by the login/register Server Actions after the
# backend issues a JWT — there's no static token to configure here anymore. See the
# "Authentication & authorization" section in the root README.
