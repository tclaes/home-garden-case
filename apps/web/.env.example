# Base URL of the Home Garden API.
API_BASE_URL=http://localhost:3000

# Optional bearer token sent as `Authorization: Bearer <token>`. The bundled backend doesn't
# currently validate it (no auth plugin registered), but the client already wires it through
# server-side only — see the "Authentication & authorization" section in the root README.
# API_AUTH_TOKEN=
